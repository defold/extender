# This test checks that we can build with an app manifest
# the extension libraries are simly duplicates, thus causing a linker error
# The manifest will resolve this by simply removing one of the libraries