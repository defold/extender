{{FRAMEWORKOPT}} module {{MODULE_ID}} {
  umbrella header "{{UMBRELLA_HEADER}}"
  export *
  {{SUBMODULE}}
}

module {{MODULE_ID}}.Swift {
  {{#HEADERS}}
  header "{{{.}}}"
  {{/HEADERS}}
  export *
  requires objc
}
