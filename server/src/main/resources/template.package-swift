// swift-tools-version: 5.9
// Generated by Extender from the uploaded SwiftPackages.json manifests.
import PackageDescription

let package = Package(
    name: "{{AGGREGATOR_NAME}}",
    platforms: [
        .{{SPM_PLATFORM}}("{{PLATFORM_MIN_VERSION}}")
    ],
    products: [
        .library(
            name: "{{AGGREGATOR_NAME}}",
            type: .static,
            targets: ["{{AGGREGATOR_NAME}}"]
        )
    ],
    dependencies: [
{{#PACKAGES}}
        .package(url: "{{URL}}", {{{REQUIREMENT}}}),
{{/PACKAGES}}
    ],
    targets: [
        .target(
            name: "{{AGGREGATOR_NAME}}",
            dependencies: [
{{#PRODUCT_DEPS}}
                .product(name: "{{PRODUCT}}", package: "{{PACKAGE_LABEL}}"),
{{/PRODUCT_DEPS}}
            ],
            path: "Sources/{{AGGREGATOR_NAME}}"
        )
    ]
)
